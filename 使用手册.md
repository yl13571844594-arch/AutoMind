# AutoMind 使用手册

> 适用版本：v1.5.2 ｜ 本手册面向零基础用户，同时包含各功能深度解读与场景建议。

> **v1.2 桌面版**：
> - **🖥 Windows 桌面版**：安装包双击即用 —— 独立窗口（WebView2）+ 系统
>   托盘，无需 Python 环境与命令行；关窗最小化到托盘，任务与定时继续跑。
>   数据存放于 `%APPDATA%\AutoMind`，托盘菜单可一键打开。自行构建见
>   仓库 `desktop/README.md`。

> **v1.1 稳定性与体验升级**：
> - **🗄 SQLite 存储引擎**：任务历史 / 对话会话 / 团队任务 / 每日限额 / 知识库
>   全部从 JSON 平面文件迁移到 SQLite（WAL 并发安全、增量写入）；首次启动
>   自动导入旧数据，旧 JSON 文件保留作备份，升级零感知，见 10.5。
> - **🎨 界面视觉精修**：玻璃拟态侧边栏与顶栏、居中阅读列、渐变导航高亮、
>   气泡阴影与悬浮反馈、发光发送按钮 —— 更现代、更通透。
> - **📖 手册增强**：内置使用手册新增目录搜索与滚动高亮、「数据与存储」章节。

> **v1.0 正式版四大升级**：
> - **🖥 界面全面重构（React 18 + Ant Design）**：前端整体迁移为现代化工程
>   （React 18 + TypeScript + Vite + Ant Design + Zustand），视觉与交互全面升级；
>   全部既有功能等价保留，操作位置不变 —— 老用户零学习成本。若未构建新前端，
>   自动回退经典界面，见 8.16。
> - **📚 RAG 知识库（社区版免费）**：侧边栏新增「📚 知识库」—— 上传
>   PDF / Word / Markdown / TXT，自动分段 + embedding，**对话时自动检索**你的
>   资料作答。社区版 5 文档/10MB；专业版无限文档/200MB/多库/Reranker/引用溯源/
>   定时重嵌入/外部向量后端；企业版再加混合检索/热度统计/检索审计/批量导入，见 8.17。
> - **⚡ 语义缓存 + 🧭 模型智能路由（专业版起）**：相似问题秒回缓存答案（0 Token）；
>   按任务复杂度自动分级选模型（专业版 2 级 / 企业版 N 级），企业版另有
>   💰 成本仪表盘（模型成本 + 缓存节省分析），见 8.18。
> - **📏 版本限额**：社区版每日任务 100 次、工作区 3 个；专业版任务不限、
>   工作区 30 个；企业版全部不限。顶栏徽标实时显示当日用量（如 `12/100`）。

> **v0.9 三大能力落地**：
> - **📄 代码编辑器（Web IDE）**：右栏新增「📄 代码」标签 —— 文件树 + Monaco
>   编辑器 + 改动 Diff 预览，浏览器直接改代码（Ctrl+S 保存，改动可撤销），
>   不影响聊天区与其他功能，见 8.12。
> - **👥 团队协作**：任务分配看板（待办/进行中/已完成）、操作实时通知
>   （同事的 Agent 改了文件 → 弹提醒）；工作区/模板/专家为服务器级存储，
>   同一部署天然团队共享，见 8.13。
> - **🎓 专家系统与专家市场**：官方精选 10 个专家一键安装、自建专家
>   （社区版最多 3 个）、激活后所有任务带角色设定执行；专业版解锁无限创建/
>   团队分享/JSON 导入导出/使用统计，企业版含审批发布流程，见 8.14。

> **v0.8 商业功能扩容**（社区版功能不减）：
> - **专业版新增**：⭐ 自定义模板（常用任务存为模板，模板库统一管理，上限 100 个）、
>   📄 审计报告导出 PDF（审计视图右上「导出报告」）。
> - **企业版新增**：🔐 SSO/LDAP 单点登录（会话令牌直通既有鉴权与 WebSocket）、
>   🧩 细粒度权限 RBAC（admin/developer/viewer 角色矩阵，可自定义）、
>   🚪 私有模型网关（LLM 出口收敛 + 模型白名单，停用一键恢复原地址）。
> - 社区版访问以上入口显示 🔒 并给出升级提示，其余功能完全不受影响。
> - **社区版同步新增**：🔌 Agent 集成 —— OpenAI 兼容接口（`/v1/chat/completions`，
>   SSE 流式）+ Continue.dev（VS Code/JetBrains 面板）一键配置生成，见 8.11。

> **v0.7 体验升级**：
> - **🌓 浅色模式**：随时切换主题（现位于左下角「⚙ 设置」菜单），首次访问跟随系统偏好。
> - **🗂 工作区**：点击顶部「🗂」徽标管理多个工作区（每个 = 独立目录 + 独立上下文），
>   切换后 Agent 在新目录下操作，各工作区的会话与任务互不污染。
> - **📚 模板库**：内置 10 个基础模板，欢迎页「快速开始」或顶部 📚 按钮一键填入。
> - **🧭 新手引导**：首次打开自动弹出 4 步引导（可在左下角「⚙ 设置」菜单重看）。
> - **💰 实时成本**：右栏 Token 面板显示预估花费（点击数字可自定义单价）。
> - **📜 历史可回溯**：任务历史持久化（重启不丢），支持查看完整产出/复制/一键重跑。
> - **▶ 中断续跑**：任务中断或出错后点「继续此任务」从断点继续，不重做已完成部分。
> - **↩️ 撤销回滚**：右栏「文件改动」列出 Agent 改过的文件，可单个撤销或全部回滚。

> **v0.6 版本体系更新（社区版 / 专业版 / 企业版）**：
> - AutoMind 采用**开源核心**模式：开源部分为**社区版**（MIT，免费），包含
>   对话/工作/编程三种模式与全部工具、技能、MCP、记忆、安全能力；
>   **🤝 协同模式、🔁 循环模式、⏰ 定时任务、📊 高级统计仪表盘**为**专业版**功能，
>   **👥 多用户会话池（执行态隔离）**为**企业版**功能，由商业扩展包 `automind-pro` 提供。
> - 安装 `automind-pro` 并配置许可证（环境变量 `AUTOMIND_LICENSE` 或项目根目录
>   `.automind_license` 文件）后重启服务自动激活；无许可证时以社区版运行，
>   对应功能入口显示 🔒 并给出升级提示，其余功能完全不受影响。
> - 界面左上角显示当前版本徽标；`GET /api/health` 返回 `edition` 字段可供确认。
> - **安全能力（鉴权/限流/密钥脱敏/安全审计）全部保留在社区版**，不设付费墙。
>   功能对比与升级说明详见 `docs/EDITIONS.md`。
>
> **v0.5 性能与多用户更新**：
> - **执行态多用户隔离（Session-Agent-Pool）**：多个用户/会话并发执行任务时各自拥有独立的
>   执行体，互不干扰计划与状态（v0.6 起归入企业版；激活后默认开启，设
>   `AUTOMIND_SESSION_POOL=0` 可关闭）。
> - **长对话流畅**：消息列表采用浏览器级"虚拟滚动"（`content-visibility`），只渲染可视区，
>   历史再长也不卡；流式输出**节流增量渲染**，长回复不再随长度变慢。
> - **可观测性**：向量库（ChromaDB）异常不再静默吞没，转为结构化日志。
>
> **v0.4 安全与稳定性更新**：修复多处前端 XSS、WebSocket 断线**指数退避重连**、消息渲染**内存泄漏**、
> 任务出错/取消时**流式气泡清理**、localStorage **容量上限**；后端**安全响应头**（CSP 等）、
> **真实语义嵌入**、CLI **`--restore` 检查点恢复**、生命周期迁移到 lifespan。均为内部加固，日常体验不变。
>
> **提示**：若命令行运行 `python -m automind.server` 报 `SyntaxError`，通常是代码里残留了
> Git 合并冲突标记（`<<<<<<<` / `=======` / `>>>>>>>`）——请拉取最新版或删除这些标记行。

---

## 目录

1. [产品简介](#1-产品简介)
2. [快速上手（零基础五分钟）](#2-快速上手零基础五分钟)
3. [界面总览](#3-界面总览)
4. [五种交互模式深度解读](#4-五种交互模式深度解读)
5. [工具审批与安全体系](#5-工具审批与安全体系)
6. [模型配置详解](#6-模型配置详解)
7. [工具面板：工具 / 技能 / MCP / 插件](#7-工具面板工具--技能--mcp--插件)
8. [高级功能](#8-高级功能)
9. [命令行与 REPL](#9-命令行与-repl)
10. [部署与运维（团队/生产）](#10-部署与运维团队生产)
11. [常见问题 FAQ](#11-常见问题-faq)
12. [附录：速查表](#12-附录速查表)
13. [更新日志](#13-更新日志)

---

## 1. 产品简介

**AutoMind 是一个"会自己动手干活"的 AI 智能体（Agent）平台。**

普通 AI 聊天工具只能"说"；AutoMind 除了聊天，还能**真正在你的电脑上执行操作**：
创建文件、修改代码、运行命令、抓取网页、生成报告——并且在做每一件事之前，
先自己**规划步骤**，做完后自己**检查结果**，出错时自己**纠正重试**。

### 它能帮你做什么？

| 你想做的事 | AutoMind 怎么帮你 |
|-----------|------------------|
| 问问题、写文案、翻译 | 💬 对话模式，像聊天软件一样直接问 |
| 「帮我建一个 Python 项目」 | ⚙️ 工作模式：自动建目录、写配置、装依赖 |
| 「修复 main.py 里的 bug」 | 💻 编程模式：读代码 → 分析 → 改代码 → 验证 |
| 「写一份行业调研报告」 | 🤝 协同模式：研究员+编写员+审阅员多角色协作 |
| 「持续优化这段代码直到测试全过」 | 🔁 循环模式：反复"执行→检查→改进"直到达标 |
| 「每天早上 9 点汇总昨日日志」 | ⏰ 定时任务：后台自动执行并记录结果 |

### 核心特点

- **本地运行、数据不上传**：所有配置、聊天记录、API Key 都存在你自己的电脑上。
- **安全可控**：每次工具调用都经过风险评估与审批门控，高危操作必须你亲自批准。
- **模型自由**：支持 OpenAI / Claude / DeepSeek / Kimi / 百炼(通义) / 智谱 / 豆包 /
  Gemini / Grok / 本地 Ollama，以及任意 OpenAI 兼容中转代理。
- **可扩展**：技能（Skill）、MCP 服务器、插件（Plugin）三套扩展体系。

---

## 2. 快速上手（零基础五分钟）

### 第 1 步：安装

前提：电脑已安装 **Python 3.11 或更高版本**（命令行输入 `python --version` 可查）。
三种安装方式任选其一：

**方式 A · 从 PyPI 安装（推荐，最简单）**

```bash
pip install "automind-agent[web]"
```

装完即可用 `python -m automind.server` 启动，或 `automind` 进入命令行模式。
升级到新版本：`pip install -U "automind-agent[web]"`。

**方式 B · 从 GitHub 源码安装（想改代码 / 参与贡献）**

```bash
git clone https://github.com/yl13571844594-arch/AutoMind.git
cd AutoMind
pip install -e ".[web]"
```

`-e` 为可编辑安装——改动源码立即生效，适合二次开发；参与贡献请先阅读
仓库中的 `CONTRIBUTING.md`。

**方式 C · 已有源码目录（压缩包 / 网盘分发）**

在软件目录打开命令行（地址栏输入 `cmd` 回车），执行 `pip install -e ".[web]"`。

> 以上 `[web]` 包含 Web 工作台所需依赖（FastAPI/uvicorn/openai 兼容后端）；
> 想使用 Claude / Gemini 官方 SDK 等全部模型后端，把 `[web]` 换成 `[full]`。
> 国内网络提速：命令末尾加 `-i https://pypi.tuna.tsinghua.edu.cn/simple`。

### 第 2 步：启动

三种方式任选其一：

| 方式 | 操作 | 适合 |
|------|------|------|
| **🖥 桌面版（推荐）** | 安装 `AutoMind-Setup-*.exe` 后双击图标，独立窗口 + 托盘 | 无需 Python 环境 |
| **Windows 一键启动** | 双击目录中的 `launch.bat`，选 `[2]` 自动打开浏览器 | 最简单 |
| **命令行** | `python -m automind.server --port 8765`，然后浏览器打开 `http://localhost:8765` | 通用 |
| **Docker** | `docker compose up --build`，访问 `http://localhost:8765` | 服务器部署 |

### 第 3 步：配置模型（只需一次）

首次打开界面，点击左下角 **「⚙ 设置」→「🔑 API Keys」**：

1. 找到你有账号的提供商（比如 DeepSeek），把 API Key 粘贴进去，点保存。
2. 点 **「🔌 测试连接」**，看到绿色通过即配置成功。
3. 顶部的模型徽标会变成绿色「已连接」。

> 没有任何 API Key？两个零成本方案：
> - **本地 Ollama**：安装 [Ollama](https://ollama.com) 并 `ollama run llama3.2`，无需 Key。
> - **中转代理**：在 API Keys 面板底部「自定义 OpenAI 标准接口」填入代理的
>   `api_base`、模型名和 Key，可对接任何 OpenAI 兼容服务。

### 第 4 步：执行第一个任务

1. 顶部模式栏点 **「⚙️ 工作」**。
2. 输入框输入：`在当前目录创建 hello.txt，内容为 Hello AutoMind`，回车。
3. 观察：聊天区出现 **「⚙️ 执行过程」面板**，实时显示 计划生成 → 逐步执行 → 完成；
   右侧「📋 执行计划」同步刷新每一步状态（○ 待执行 → ◐ 执行中 → ✓ 完成）。
4. 打开项目目录，`hello.txt` 已经真实创建。

**恭喜，你已经掌握了 AutoMind 的基本用法。** 下面的章节帮你把每个功能用到极致。

---

## 3. 界面总览

```
┌──────┬──────────────────────────────────────────────┬─────────────┐
│      │  💬对话 ⚙️工作 💻编程 🤝协同 🔁循环   ⚡自动 ▾ │             │
│ 侧   │  ●已连接  deepseek/deepseek-chat  📁项目 🗂工作区│  📊 实时状态 │
│ 边   │  📚模板  ❓引导  🔄新会话                      │  🪙 Token+💰 │
│ 栏   ├──────────────────────────────────────────────┤  📋 执行计划 │
│      │                                              │  ↩️ 文件改动 │
│ 工作区│               聊 天 消 息 区                  │  🌐 HTML预览 │
│ 系统 │                                              │  🛡️ 审计概览 │
│ ⚙设置├──────────────────────────────────────────────┤             │
│      │  📎 🎤 [输入消息，Enter 发送...........] ▶ ■  │             │
└──────┴──────────────────────────────────────────────┴─────────────┘
```

### 左侧边栏（导航）

| 入口 | 功能 |
|------|------|
| 💬 对话工作台 | 主聊天区（默认视图） |
| 📋 计划视图 | 查看当前/最近任务的分层计划树 |
| 🔧 工具面板 | 管理 工具/技能/MCP/插件 四类能力 |
| 📈 观测中心 | 执行流程可视化（DAG）+ 实时看板：社区版可看**当前任务**的实时 DAG，专业版加历史与看板（见 8.20） |
| 📊 统计分析 | 基础统计（社区版）+ 高级仪表盘：命中率圆环、趋势图、Token 效率（专业版） |
| ⏰ 定时任务 | 创建与管理周期性自动任务（专业版，社区版显示 🔒） |
| 📜 任务历史 | 最近执行过的任务列表与结果 |
| 🎓 专家市场 | 官方精选/自建专家，激活后任务带角色设定执行（见 8.14） |
| 👥 团队 | 任务分配看板 + 操作实时通知（见 8.13） |
| 🛡️ 安全审计 | 每次工具调用的风险评估与授权记录 |
| ⚙ 设置（左下角） | 弹出设置菜单：模型/Keys/通用/集成/主题/工作区/引导/手册 |

### 顶部栏（控制）

- **模式切换（分组式）**：默认只显示 3 个**主模式**——💬 对话 / ⚙️ 工作 / 💻 编程，
  界面清爽、新手不慌；主模式右侧有一条分隔线和「**⋯ 高级**」按钮，点开后展开
  🤝 协同 / 🔁 循环两个**高级模式**（老用户全火力）。选中某个高级模式时会自动展开，
  确保当前模式始终可见。每个模式的**会话内容互相独立**，切换不会丢失各自的聊天记录。
- **审批下拉**：🙋询问 / ⚡自动 / ✅全批准（详见第 5 章）。
- **状态徽标**：`●已连接`（WebSocket 状态）、`deepseek/xxx`（当前模型，绿=就绪 黄=异常 红=未配置）、
  `📁项目名`（当前项目目录，点击可更换）、`🗂工作区`（点击管理/切换工作区，见 8.7）。
- **📚 模板**：打开模板库（内置 10 个基础模板；专业版可管理自定义模板，见 8.8）。
- **❓ 引导**：重看 4 步新手引导（首次打开会自动弹出）。
- **🔄 新会话**：清空当前模式的对话（其他模式不受影响）。

### ⚙ 设置菜单（左下角）

其余设置功能收纳在**侧边栏左下角「⚙ 设置」**，点击弹出菜单，每项一个功能：

| 菜单项 | 功能 |
|--------|------|
| 🖥 模型配置 | 全局/分模式选择模型、采样参数（见 6.2） |
| 🔑 API Keys | 各提供商 Key 管理与连接测试（详见第 6 章） |
| ⚙ 通用设置 | 项目目录、Temperature、最大 Token、自主闭环开关（见 6.3） |
| 🔌 Agent 集成 | Continue.dev / OpenAI 兼容 API 一键配置（见 8.11） |
| 🌓 切换主题 | 深色 ⇆ 浅色，持久化记忆，首次访问跟随系统偏好 |
| 🗂 工作区管理 | 添加/切换/删除工作区（见 8.7） |
| 🧭 新手引导 | 重看 4 步引导（首次打开会自动弹出） |
| 📖 使用手册 | 新标签页打开本文档（`/manual`，离线可用） |

### 输入区

- `Enter` 发送，`Shift+Enter` 换行。
- **📎 图片**：上传图片给视觉模型看图问答（对话模式）。
- **🎤 语音**：浏览器语音转文字输入。
- **■ 停止**：中断正在执行的任务（快捷键 `Ctrl+.`）。

### 右侧面板（观测）

任务执行时这里是"仪表盘"：实时步骤数/回溯数/Token/耗时、Token 累计用量与
**💰 预估成本**（按当前模型单价估算，点击成本数字可自定义单价）、
分层计划树实况、**↩️ 文件改动**（Agent 改过的文件列表，可单个撤销或全部回滚）、
项目中 HTML 文件的一键预览、最近工具调用的审计摘要。

### 键盘快捷键（v1.4 起为完整快捷键系统）

macOS 上把下表的 `Ctrl` 读作 `⌘`。应用内按 <kbd>?</kbd> 可随时调出这张表
（侧栏「⚙ 设置 → ⌨ 键盘快捷键」也能打开），内容与实际生效的按键同源。

**通用**

| 快捷键 | 功能 |
|--------|------|
| `Enter` / `Shift+Enter` | 发送 / 换行 |
| `Esc`（或 `Ctrl+.`） | 停止当前任务；有弹窗时先关弹窗 |
| `Ctrl+N`（或 `Ctrl+L`） | 新会话（清空当前模式对话，会二次确认） |
| `Ctrl+K` | 打开模板库 |
| `Ctrl+,` | 打开通用设置 |
| `Ctrl+Shift+W` | 切换工作区 |
| `Ctrl+/` | 光标定位到输入框 |
| `Ctrl+Shift+L` | 切换深色 / 浅色主题 |
| `Ctrl+=` / `Ctrl+-` / `Ctrl+0` | 增大 / 减小 / 复位字号 |
| `?` | 显示快捷键帮助 |

**切换模式**：`Ctrl+1` 对话 · `Ctrl+2` 工作 · `Ctrl+3` 编程
**切换视图**：`Alt+1` 对话工作台 · `Alt+2` 计划 · `Alt+3` 工具 · `Alt+4` 历史

> 说明：带修饰键的组合在输入框里照常生效，不会打断打字；`?` 这类单字符快捷键
> 只在非输入状态生效，否则你就打不出问号了。中文输入法组词过程中不会被抢键。

### 界面偏好（字号 / 通知 / 动效）

在「⚙ 设置 → ⚙ 通用设置 → 🎨 界面偏好」里，三项都是**本机生效、立即生效、
自动记住**，不改变任何功能，也不会同步到别的机器：

| 项目 | 说明 |
|------|------|
| **字号** | 85% ~ 130% 无级调节，界面整体等比缩放（含 antd 组件）。也可用 `Ctrl+=` / `Ctrl+-` / `Ctrl+0` |
| **任务完成通知** | 任务跑完发系统通知，**只在窗口不可见时**提醒；点通知直接跳回应用 |
| **减少动效** | 关闭淡入等动画，老机器上更跟手；系统级「减少动态效果」也会被自动尊重 |

**关于通知权限**：首次勾选「任务完成后发送系统通知」时浏览器/系统会询问授权，
必须允许才会收到提醒。若曾误点"拒绝"，需先到浏览器或系统的通知设置里放行
AutoMind，再回来重新勾选。桌面版会直接使用系统通知中心。

---

## 4. 五种交互模式深度解读

### 💬 对话模式 — "问答专家"

**原理**：纯多轮对话，不调用任何工具，直接与大模型交互，响应最快。
支持图片输入（需要视觉模型，如 gpt-4o / claude / qwen-vl）。

**适合场景**：
- 知识问答、概念解释、头脑风暴
- 文案撰写、翻译、润色
- 上传截图/图表提问（「这张报错截图是什么问题？」）

**不适合**：需要动手操作文件/命令的任务（它没有"手"，请切工作或编程模式）。

**小技巧**：对话历史按浏览器隔离持久化，刷新页面不丢；「🔄 新会话」可随时清空重来。

---

### ⚙️ 工作模式 — "项目经理 + 执行团队"

**原理**：Plan-and-Execute 架构。收到任务后：
1. **分层规划**：把大任务拆成目标树（比如「建博客项目」→ 建目录 → 写配置 → 初始化 git → 写首页）；
2. **依赖排序**：自动分析步骤间的依赖（先建目录才能写文件），生成正确执行顺序；
3. **逐步执行**：每一步调用相应工具（终端/文件/浏览器），执行前过权限门控；
4. **符号验证**：每步完成后校验预期效果是否达成；
5. **自我纠错与回溯**：某步失败时让模型分析错误并生成修正方案重新执行，仍不行则回溯调整计划。

**适合场景**：
- 「初始化一个 FastAPI 项目，带健康检查接口和 Dockerfile」
- 「把这个目录下所有 CSV 合并成一个 Excel 并生成汇总图表」
- 「下载 XX 页面的数据并整理成 Markdown 表格」
- 一切**多步骤、有先后依赖**的自动化任务

**观察窗口**：执行时聊天区的「⚙️ 执行过程」轨迹卡片（紫色=计划、绿色=工具调用、黄色=回溯）
和右栏「📋 执行计划」树是理解它"在想什么"的最佳窗口。

**建议**：任务描述越具体，规划质量越高。给出目标目录、技术栈、验收标准会显著提升成功率。

---

### 💻 编程模式 — "结对程序员"

**原理**：ReAct（思考→行动→观察）循环。模型每轮先输出**思考**，
再决定调用什么工具（读文件/改代码/跑命令），观察结果后进入下一轮，
直到判断任务完成。相比工作模式，它**不预先定死计划**，边做边调整，更适合探索性任务。

**适合场景**：
- 「读一下 src/main.py，找出为什么启动报错并修好」
- 「给 utils.py 里的函数补全类型注解和 docstring」
- 「写单元测试覆盖 order 模块，然后跑通」
- 重构、调试、代码审查等**需要反复看代码-改代码-验证**的工作

**执行可视化**：思考（蓝色卡片）与工具调用（绿色卡片）实时流式展示，
你能看到它每一步"为什么这么做"。

**建议**：先在「⚙ 设置」中把**项目目录**指到你的代码仓库根目录，
它读写文件都以此为根（也受路径防护限制，不会越界操作）。

---

### 🤝 协同模式 — "多角色专家组"（专业版）

> 🔒 本模式为**专业版**功能：社区版中按钮显示 🔒，点击会提示升级；
> 安装 `automind-pro` 并配置许可证后自动解锁。

**原理**：多智能体编排。系统把任务分派给多个角色（规划者/研究员/编程者/审阅者），
各自基于共享白板完成子任务，最后综合产出。角色间互相"看得到"彼此的成果。

**适合场景**：
- 「调研三个开源向量数据库的优劣并给出选型建议」（研究员搜集→审阅者把关）
- 「设计一个签到系统：给出方案、代码骨架和风险评估」（跨角色长任务）
- 需要**多视角**（技术+产品+风险）综合判断的复杂问题

**观察窗口**：聊天区会显示「🤝 多智能体协同」进度面板，逐角色展示分工与产出。

---

### 🔁 循环模式 — "不达目标不罢休"（专业版）

> 🔒 本模式为**专业版**功能，解锁方式同上。
> 社区版的工作/编程模式仍内置「自主任务闭环」（§8.0），基础的自动验收与修复不受影响。

**原理**：Loop Engineering 自主闭环：执行 → 用模型判断"目标达成了吗" → 未达成则带着
反馈继续下一轮。内置**四重停止条件**防止无限循环：
1. **语义完成**：模型判定任务已达标；
2. **输出收敛**：连续两轮输出高度相似（相似度>0.95），说明已无改进空间；
3. **连续空转**：两轮没有执行任何实际操作；
4. **最大轮数**：兜底上限。

**适合场景**：
- 「持续优化这个函数的性能，直到基准测试 <100ms」
- 「反复运行测试并修复失败项，直到全部通过」
- 「把这篇文章改到 500 字以内且保留所有要点」
- 一切**有明确验收标准、需要迭代逼近**的任务

**观察窗口**：「🔁 循环工程」面板显示每轮的动作与判定结论及停止原因
（完成/收敛/空转/上限）。可随时点 ■ 中断。

**建议**：任务里**写清楚验收标准**（"直到测试全过"、"直到没有 lint 错误"），
判定器才有明确依据。

---

### 模式选择速查

```
只是想问/写点东西？            → 💬 对话
让它替我做一件多步骤的事？      → ⚙️ 工作
围绕代码读改测？               → 💻 编程
复杂问题想要多角度方案？        → 🤝 协同（专业版）
有明确标准要迭代到达标？        → 🔁 循环（专业版）
```

---

## 5. 工具审批与安全体系

AutoMind 的一大特色是**每次工具调用都被安全体系拦截评估**，你可以精确控制"它能做什么"。

### 5.1 三种审批模式（顶部下拉切换）

| 模式 | 行为 | 适合场景 |
|------|------|---------|
| 🙋 **询问** | 除只读操作（读文件、查目录）外，**每次**工具调用都弹窗请你批准 | 首次使用、操作重要项目、想完全掌控 |
| ⚡ **自动**（默认） | 低风险自动放行；风险分 ≥60 的敏感操作和所有高危操作弹窗确认 | 日常使用的最佳平衡 |
| ✅ **全批准** | 跳过所有审批，完全自主运行 | 沙箱环境跑批量任务；**慎用** |

弹窗出现时会显示：工具名、参数、风险等级与原因，你选择「批准」或「拒绝」。
拒绝后模型会收到反馈并尝试其他途径或放弃该步。

### 5.2 权限分级（系统内部如何评估）

每个工具自带权限等级 + 每次调用动态评估风险分（0-100）：

- **safe（安全）**：读文件、列目录、`git status` 等只读操作 → 直接放行；
- **sensitive（敏感）**：写文件、装依赖、`git commit` 等 → 按审批模式处理；
- **dangerous（高危）**：`rm -rf`、`sudo`、强制推送、格式化等 → 必须人工批准。

另有多重硬防护独立于审批模式**始终生效**：
- **命令注入拦截**：`rm -rf /`、fork bomb、写磁盘设备等灾难性命令直接拒绝执行；
- **路径防护**：文件读写被限定在项目目录内，`../` 越界访问直接拒绝；
- **Python 沙箱**：代码执行工具只允许白名单模块（math/json/re 等），禁止 os/subprocess。

### 5.3 审计日志（🛡️ 安全审计）

每次工具调用（无论放行或拒绝）都被记录：时间、工具、参数摘要、决策、风险分、原因。
侧边栏「🛡️ 安全审计」查看完整记录与汇总（放行/询问/拒绝/高危次数）。
**用途**：事后追溯"它到底做过什么"，尤其在全批准模式跑完任务后审查。

> 📄 **导出报告（专业版）**：审计视图右上「导出报告」可把审计日志导出为
> 正式 PDF 报告（装有 reportlab 时直接出 PDF，否则给出打印版页面，
> 浏览器「另存为 PDF」即可），用于交付审查或合规存档。

---

## 6. 模型配置详解

### 6.1 API Keys 面板（右上角 🔑）

支持的提供商与获取 Key 的去处：

| 提供商 | 默认模型 | 说明 |
|--------|---------|------|
| OpenAI | gpt-4o | platform.openai.com |
| Anthropic (Claude) | claude-sonnet | console.anthropic.com |
| DeepSeek | deepseek-chat | platform.deepseek.com，性价比高 |
| Kimi (Moonshot) | moonshot-v1 | platform.moonshot.cn |
| 百炼 (通义/Qwen) | qwen-max | dashscope.aliyun.com |
| 智谱 (GLM) | glm-4 | open.bigmodel.cn |
| 豆包 (Doubao) | doubao | volcengine.com |
| Gemini | gemini-pro | ai.google.dev |
| Grok | grok-3 | x.ai |
| **Ollama（本地）** | llama3.2 | **无需 Key**，本机 `ollama serve` 即可 |
| **自定义（中转代理）** | 自填 | 任意 OpenAI 兼容 `/v1/chat/completions` 服务 |

- Key 只保存在本地 `.automind_config.json`，不会上传任何服务器。
- 也可用环境变量：`OPENAI_API_KEY`、`DEEPSEEK_API_KEY`、`MOONSHOT_API_KEY`、
  `DASHSCOPE_API_KEY`、`ZHIPU_API_KEY` 等。
- **🔌 测试连接**：逐阶段验证（解析→连接→认证→响应），排障利器。

### 6.2 模型选择（⚙ 设置 → 🖥 模型配置）

- 全局默认模型：所有模式的兜底选择。
- 每个提供商可**自定义模型名**（输入框直接填任意模型，如新发布的型号）。
- **各模式专用模型**：可为五种模式分别指定不同模型——比如
  对话用便宜快的 `deepseek-chat`，编程用强力的 `claude-sonnet`，实现**成本/能力分配**。
  未指定的模式沿用全局默认。

### 6.3 通用设置（⚙ 设置 → 通用设置）

- **项目目录**：Agent 一切文件操作的根（带可视化目录浏览器，支持盘符切换）。
  **强烈建议**为不同项目切换对应目录，既准确又安全。
- **Temperature**（0-2）：越低越严谨（代码任务建议 0-0.3），越高越发散（创意任务 0.7+）。
- **最大输出 Token**：单次回复长度上限。

---

## 7. 工具面板：工具 / 技能 / MCP / 插件

侧边栏「🔧 工具面板」下有四个分段，对应四类能力：

### 7.1 🔧 工具（Agent 的"手"）

内置工具一览：

| 工具 | 等级 | 功能 |
|------|------|------|
| `terminal` | 敏感 | 执行命令行（装依赖、跑脚本、git 操作） |
| `file_read` | 安全 | 读取文件内容 |
| `file_write` | 敏感 | 创建/覆盖文件 |
| `file_edit` | 敏感 | 精确字符串替换编辑（带 diff 与回滚） |
| `python_sandbox` | 敏感 | 沙箱内执行 Python 片段（计算/数据处理） |
| `web_fetch` | 安全 | 抓取网页并提取正文与链接 |
| `browser` | 敏感 | Playwright 浏览器自动化（导航/点击/输入/截图） |

每个工具卡片有**开关**：临时关掉某工具，Agent 执行时就"看不到"它。
**场景**：不想让它上网 → 关掉 `web_fetch` 和 `browser`；只读分析 → 只留 `file_read`。

### 7.2 ✨ 技能（预制的"专家流程"）

技能 = 封装好的多步专业流程，模型可一键调用。内置 6 个：

| 技能 | 功能 | 典型用法 |
|------|------|---------|
| `project_init` | 项目脚手架（目录/pyproject/venv/git） | 「初始化一个新项目」 |
| `code_generator` | 代码生成/**补全**/脚手架，带语法校验与自动修复 | 「生成一个爬虫脚本」 |
| `test_runner` | 发现并运行测试、收集失败 | 「跑一下测试」 |
| `log_analyzer` | 日志级别统计/错误样本/高频模式聚类 | 「分析这个日志文件」 |
| `doc_generator` | 从 Python 源码生成 Markdown API 文档 | 「给 src 目录生成 API 文档」 |
| `dep_audit` | 依赖审计：未固定版本/重复声明/已装版本 | 「检查项目依赖健康度」 |

**导入自定义技能**（两种格式）：
- **SKILL.md 技能包**：文件夹内放一个 `SKILL.md`（YAML 头写 name/description/emoji，
  正文写指令），适合无代码的提示词型技能。「📁 加载目录」批量导入。
- **Python 技能**：继承 `AbstractSkill` 的 `.py` 文件，「📄 导入 .py」。
  开发模板见 `examples/03-skill-development/`。
- 「⬇️ 一键导入桌面 skills 文件夹」可直接吸收桌面的技能库。

### 7.3 🔌 MCP（连接外部工具生态）

MCP（Model Context Protocol）是开放协议，接入后外部服务的工具会
**自动出现在工具列表里**供 Agent 调用（文件系统、数据库、搜索、地图……）。

- **单个添加**：名称 + 传输方式（stdio 本地命令 / sse URL）+ 命令与参数。
- **批量导入**：直接粘贴 Claude Desktop 格式的 `{"mcpServers": {...}}` JSON。
- 需先 `pip install mcp`（面板会提示）。
- 连接状态、每个服务器暴露的工具一目了然，可随时删除。

### 7.4 🧩 插件（生命周期钩子扩展）

插件在任务**开始/结束/解析后/计划后/出错**等时机自动执行你的自定义逻辑
（记日志、发通知、审计上报），**不改动主程序**。

- 插件放在 `~/.automind/plugins/<名称>/`，含 `plugin.json`（元信息）+
  `hooks.py`（提供 `get_hooks() → AgentHooks`）。
- 面板中点「加载/卸载」即时生效；「🔄 重新扫描」发现新插件。
- 插件内部异常会被系统吞掉，**永远不会影响任务主流程**。
- 完整开发示例见 `examples/04-plugin-development/`（task-timer 计时插件）。

---

## 8. 高级功能

### 8.0 🔄 自主任务闭环（v0.3 新增，默认开启）

**做什么**：工作 / 编程模式的任务完成后，系统自动执行一条"质检流水线"，
不合格就**自动返工**——把"AI 说做完了"变成"验证过真的做完了"。

```
任务执行完成
   │
   ▼
🧪 TDD 测试（编程模式）      项目有 tests/ 时自动跑 pytest
   │                        （执行中每次改 .py 还会即时语法验证）
   ▼
🧐 多 Agent 审查（工作模式）  审阅者角色复核结果，可调用只读工具核实
   │                        （工具共享同一注册表，MCP 工具同样可用）
   ▼
✅ Loop 语义验收             判定"任务是否真正完成"
   │
   ├─ 通过 → 输出末尾附「🔄 自主闭环」摘要
   └─ 未过 → 带着反馈自动修复（最多 2 轮）后再次验收
```

**五个开关**（「⚙ 设置」→ 通用 → 自主任务闭环，默认全开、即时生效）：

| 开关 | 作用 | 关闭的场景 |
|------|------|-----------|
| 🧐 多Agent审查 | 工作模式完成后审阅者复核 | 追求极致速度 / 省 Token |
| ✅ Loop 验收 | 语义验收 + 未过自动修复（≤2 轮） | 简单任务不需要返工 |
| 🧪 TDD 测试 | 编程模式改 .py 即时语法验证 + 收尾跑 pytest | 项目测试极慢时 |
| ⚡ 并行执行 | 计划中互不依赖的步骤并发执行 | 步骤间有隐式资源冲突时 |
| 📦 子任务缓存 | 同任务内相同只读调用（读文件/抓网页）复用结果 | 需要每次强制重读时 |

**编程模式的 TDD 内环**：每次 `file_write`/`file_edit` 写入 `.py` 文件后，
系统立即做语法检查并把 `syntax_check: OK / FAILED` 注入模型的观察结果——
有语法错误它下一步就会看到并立刻修复，形成 **编辑 → 验证 → 修复** 的真正 TDD 闭环。
另有 `code_generate` 工具（背后是 code_generator 技能）：语言检测、
语法校验 + 自动修复、`mode='complete'` 代码补全。

**安全边界**：并行只发生在互不依赖的步骤之间；缓存只作用于 SAFE 级只读工具
（写文件、执行命令永远真实执行）；审阅者只能调用只读工具。

### 8.1 ⏰ 定时任务

> 🔒 **专业版**功能：社区版侧边栏入口显示 🔒，打开后为升级说明卡片。

**做什么**：让某个任务按固定周期自动执行（无需开着页面盯着）。

**创建**：侧边栏「⏰ 定时任务」→ 填任务内容 → 选模式（五种均可）→
选间隔（每5分钟/每小时/每6小时/每天）→ 添加。

**典型场景**：
- 每天汇总一次项目日志中的错误（配 `log_analyzer` 技能）
- 每小时抓取某页面数据追加到 CSV
- 每 6 小时跑一遍测试并记录结果

每次执行的结果记入任务历史；可随时暂停/启用/手动触发/删除。重启服务后自动恢复调度。

### 8.2 📊 统计分析（高级仪表盘，专业版）

> 社区版保留**基础统计**（任务总数/成功率/平均耗时/Token 累计/按模式聚合/工具排行）；
> 以下高级仪表盘为**专业版**功能，社区版页面底部会显示升级卡片。

- **四个命中率圆环**：工具命中率 / 计划命中率 / 任务成功率 / 自我修正率 + 综合平均。
- **上下文使用率发光条**：当前对话窗口占模型上下文的百分比，是否触发过压缩。
- **趋势折线**：最近 50 次任务的工具命中率变化。
- **Token 效率**：每 Token 产出的有效字符数；输入/输出分项累计。
- **记忆系统**：长期记忆文档数、知识图谱实体/关系数、短期窗口消息数。

**用途**：评估"这个模型/这种提示词效果如何"、监控 Token 花销、发现频繁回溯的异常任务。

### 8.3 🌐 HTML 预览

Agent 生成的 HTML 文件（报告/图表/小页面）出现在右栏「🌐 HTML 预览」列表，
点击即可在弹窗内直接渲染查看，无需手动找文件。

### 8.4 多模态输入

- **📎 图片**：对话模式下上传图片（可多张），配视觉模型即可看图问答、读截图、分析图表。
- **🎤 语音**：点击后说话，浏览器实时转文字进输入框（基于 Web Speech API，需浏览器支持）。

### 8.5 多用户会话隔离

每个浏览器持有独立的会话 ID：A 电脑与 B 电脑打开同一服务，聊天记录互不可见、
互不清除。团队共用一台服务器部署时无串扰。（社区版即支持）

> 👥 **执行态隔离（会话级 Agent 池）为企业版功能**：并发执行工作/编程任务时
> 每个会话拥有独立执行体（独立计划/ReAct 状态/审批回调），互不干扰；
> 激活企业版后默认开启，`AUTOMIND_SESSION_POOL=0` 可关闭。

### 8.6 输出便捷操作与富渲染

- 每条 AI 消息悬浮 **「⧉ 复制」**（复制整条 Markdown 原文）。
- 每个代码块带语言标签头和独立 **「⧉ 复制」** 按钮。
- 回答中的 Markdown **完整渲染**：标题、有序/无序列表、表格（圆角边框、
  悬停高亮、超宽自动横向滚动）、引用块、分隔线、删除线、斜体，
  深浅两种主题均适配。

### 8.7 🗂 工作区（多项目互不污染）

**每个工作区 = 独立目录 + 独立上下文。** 点击顶部 `🗂` 徽标：

- **添加**：起个名字 + 填目录绝对路径（如 `D:\projects\blog`）。
- **切换**：Agent 立即在新目录下重建（记忆/索引/文件权限根随之切换）；
  每个工作区的**会话内容、对话历史相互隔离**，A 项目的任务绝不会
  污染 B 项目的上下文。
- **回到默认**：恢复服务器启动目录。
- 删除工作区只删记录，**不动磁盘目录**。任务执行中不允许切换（先停止）。

### 8.8 📚 模板库（+ 专业版自定义模板）

- **内置 10 个基础模板**：个人主页、项目脚手架、实用脚本、修复报错、
  补单元测试、生成 README、数据分析、网页抓取、写周报、翻译润色。
  欢迎页「快速开始」区或顶部 📚 按钮打开，点击即自动切换模式并填入提示词。
- **⭐ 自定义模板（专业版）**：把常用任务保存为自己的模板（名称/图标/
  适用模式/提示词，上限 100 个）——「📥 取输入框」可把当前输入直接存为模板，
  团队沉淀提示词资产。数据存于 `.automind/custom_templates.json`。

### 8.9 ↩️ 撤销 / 回滚（放心让 Agent 改文件）

Agent 每次写入/编辑文件前都会记录**前像**（原始内容），右栏「↩️ 文件改动」：

- **单文件撤销**：恢复该文件到改动前的原始内容；**新建的文件会被删除**。
- **全部回滚**：一键恢复本次运行以来记录的全部改动（上限 200 条记录）。
- 记录保存在服务进程内存中，重启服务后清空 —— 想长期保险请配合 git。

### 8.10 ▶ 任务中断继续（不浪费 Token 重做）

任务**中断**（■ / `Ctrl+.`）或**出错**后，对话区出现「▶ 继续此任务」按钮：
带着"先检查现状、不重做已完成部分"的指令从断点继续 —— 已产出的文件
和上下文都还在，避免整段重跑烧 Token。

### 8.11 🔌 Agent 集成（Continue.dev / Cline / OpenAI 兼容 API）

AutoMind 内置 **OpenAI 兼容接口**（`/v1/chat/completions`，支持 SSE 流式），
可以把它当作"模型提供方"接入 IDE 里的 AI Agent 或任何支持自定义
OpenAI 接口的工具 —— 你在 AutoMind 配好的模型、中转代理、企业网关，
外部 Agent 直接复用，Key 只留在本机。

**Continue.dev（VS Code / JetBrains 侧边面板）三步接入：**

1. IDE 扩展市场安装 **Continue**；
2. 打开左下角「⚙ 设置」菜单 → 🔌 Agent 集成，点击「⧉ 复制配置」；
3. Continue 面板 ⚙ → Open Config，把配置粘贴进 `config.yaml` 的
   `models:` 段，保存即可在编辑器里与 AutoMind 对话、改代码。

**Cline（VS Code 自主编码 Agent）：** 安装 Cline 扩展后，在其设置中选
API Provider = `OpenAI Compatible`，Base URL / Model ID 从「🔌 Agent 集成」
面板一键复制（面板会按当前服务地址与模型自动生成）。

说明：经 /v1 接入的外部 Agent 为**纯对话语义**（不会调用 AutoMind 的本机工具，安全边界清晰）；
token 用量计入右栏统计与成本估算；开启 `AUTOMIND_AUTH_TOKEN` 后
各客户端的 `apiKey` 填同一令牌，SSO 会话令牌同样可用。
Zed / Open WebUI / 自写脚本等任何 OpenAI 客户端也能以相同方式接入
（Base URL 填 `http://localhost:8765/v1`）。

### 8.12 📄 代码编辑器（Web IDE）

右栏顶部切到 **「📄 代码」标签**，浏览器变成真正的代码编辑器（右栏自动加宽，
聊天区照常可用）：

- **文件树**：项目目录结构（自动排除 .git/node_modules/缓存等），目录可折叠；
- **Monaco 编辑器**：VS Code 同款内核，语法高亮 / 智能缩进 / `Ctrl+S` 保存
  （按需从 CDN 加载，首次需联网；离线自动回退为基础编辑器，功能不减）；
- **± Diff 预览**：被 Agent 或编辑器改过的文件可一键查看「改动前 vs 当前」对比；
- **安全**：读写严格限定在项目根目录内；每次保存的前像记入「文件改动」，
  随时可撤销回滚。

### 8.13 👥 团队协作

侧边栏 **「👥 团队」**（同一服务器部署 = 同一团队）：

- **任务分配**：填标题 + 指派成员，看板式流转 待办 → 进行中 → 已完成；
- **操作通知**：任一成员的 Agent 完成任务/修改文件，其他人实时收到弹窗提醒
  与活动流记录（含改动文件数，可去右栏查看/回滚）；
- **共享语义**：🗂 工作区（多人看同一项目目录）、⭐ 自定义模板（专业版）、
  🎓 专家（分享后全员可用）均为服务器级存储，天然团队共享；
  配合企业版 SSO/RBAC 获得成员身份与权限控制（见 10.4）。

### 8.14 🎓 专家系统与专家市场

**专家 = 可复用的角色设定**（人设 + 专业提示词）。侧边栏「🎓 专家市场」：

- **官方精选 10 个**：前端专家/后端架构师/数据分析师/爬虫专家/测试工程师/
  DevOps/文案写手/专业译者/产品经理/安全审计员 —— 一键安装；
- **激活**：点「激活」后，之后的**所有任务**（对话/工作/编程）自动带上该专家的
  角色设定执行；模式提示条会显示当前专家徽标，点 ✕ 取消；
- **创建自己的专家**：名称 + 图标 + 角色设定提示词（社区版最多 **3 个**）；
- **专业版解锁**：♾ 无限创建、👥 团队内部分享、📤📥 JSON 导入/导出
  （团队分发提示词资产）、📊 使用统计（哪个专家被调用最多）；
- **企业版**：企业专属专家市场 —— 分享的专家需**管理员审批**后全员可见
  （`/api/experts/pending` 审批队列）。

### 8.15 💰 实时成本估算

右栏 Token 面板显示**预估花费**：内置 DeepSeek/GPT/Claude/Kimi/GLM/Qwen/
豆包/Gemini 等主流模型单价表（¥/百万 token，按公开定价粗估）；
点击成本数字可**自定义单价**（例如中转代理的折扣价），留空恢复内置表。
每条任务结果的时间栏也会附带该次任务的估算成本。

### 8.16 🖥 v1.0 新前端（React 18 + Ant Design）

v1.0 起前端为 **React 18 + TypeScript + Vite + Ant Design + Zustand** 工程
（源码在仓库 `web/` 目录），构建产物随发行包内置于 `automind/static/dist`，
**开箱即用，无需 Node 环境**。全部功能与操作位置与旧版一致。

- 二次开发：`cd web && pnpm install && pnpm dev`（Vite 开发服务器，自动代理
  到本机 8765 端口的后端）；`pnpm build` 产出到 `automind/static/dist`。
- 服务端优先伺服 `static/dist/index.html`；删除 dist 目录即回退经典
  原生 JS 界面（保留在 `automind/static/`，功能一致）。

### 8.17 📚 RAG 知识库（让 Agent 用你的资料回答）

侧边栏「📚 知识库」：上传 **PDF / Word(.docx) / Markdown / TXT**，系统自动
**分段 + 向量化（embedding）**；之后在**对话模式**提问时，Agent 自动检索
最相关的片段注入上下文，用你的资料作答（可用「自动检索」开关关闭）。

- **检索测试**：视图内输入问题即可预览会检索到什么（含相关度分数）。
- **离线可用**：内置特征哈希嵌入，无需联网或额外 API Key；安装 chromadb 可选。
- **社区版**：5 个文档 / 10MB / 单一知识库。
- **专业版（rag_pro）**：无限文档 / 200MB；**多知识库**（按主题分库）；
  **Reranker 二阶段重排**（精度提升 15-20%）；**引用溯源**（回答标注
  `[编号]` 与来源文档、段落号）；**定时自动重嵌入**；可选
  **Milvus / Pinecone / Qdrant** 向量后端（需安装对应 SDK）。
- **企业版（kb_enterprise）**：总量不限；**混合检索**（向量语义 + 词法双通道，
  型号/专名等关键词精确命中）；**热度统计**（哪些文档最常被引用）；
  **检索审计日志**（合规追溯）；**服务器目录批量导入**。

### 8.18 ⚡ 语义缓存与 🧭 模型智能路由（降本增效）

侧边栏「🧭 路由与成本」（专业版起）：

- **语义缓存**：对话模式下，与历史问题**语义相似**的提问直接秒回缓存答案
  （0 Token、毫秒级）。可调相似阈值、一键清空；专业版基础缓存
  （300 条/24h），企业版高级缓存（2000 条/7 天 + 命中与节省统计）。
- **模型智能路由**：按任务复杂度（文本长度/代码特征/多步骤标志，0~100 打分）
  自动选择模型档位 —— 简单问答走便宜小模型、复杂任务走强模型。专业版
  2 级（轻量/强力），企业版 N 级自定义；「路由预演」可输入任务文本查看
  将命中哪一档。
- **成本仪表盘（企业版）**：按模型/日聚合 Token 用量与估算成本，
  叠加语义缓存节省额度，成本一目了然。

### 8.20 📈 观测中心（执行流程可视化 + 实时看板）

左侧边栏 **📈 观测中心** —— 把 Agent「正在做什么、做到哪一步、卡在哪里」
画成一张实时更新的图，不必再从滚动的日志里找线索。

**社区版（免费即可用）：当前任务的实时 DAG**

- **实时**：图随事件流即时刷新，无需手动刷新页面；执行中的节点有呼吸动效；
- **看得懂结构**：任务 → 计划步骤 → 工具调用三层。步骤按计划树的**真实层级**
  并列展开（同级步骤常并行执行，故不会画成一条假的先后链）；工具调用以虚线
  挂在其所属步骤右侧；
- **状态一目了然**：待执行 / 执行中 / 成功 / 失败 / 回溯 / 已中断 六种配色；
- **点节点看详情**：类型、所用工具、耗时、失败原因（失败节点直接给出错误摘要）；
- **实时计数**：步骤数、工具调用数、失败数、回溯数、总耗时；
- **限制**：只保留**当前任务**（新任务开始即替换），**只读**，不落盘、无历史、
  无聚合看板。刷新页面会自动从服务端补回当前任务的图。

**专业版：实时看板 + 运行历史**

- **实时看板**：可选 1 小时 / 24 小时 / 7 天 / 30 天窗口，统计运行次数、成功率、
  平均耗时与 **P50 / P95 分位**、总步骤与总工具调用；
- **工具热度与失败率**：哪个工具调用最频繁、哪个最容易失败，一眼定位薄弱环节；
- **失败归因 Top**：把失败原因聚类计数，反复出现的问题自动浮上来；
- **运行历史**：留存最近 200 次运行，可点「回看」把任意一次的完整 DAG 重新画出来；
- **导出 JSON**：一键导出运行记录，便于外部分析或留档。

**企业版**：留存扩大到 2000 次，并增加**按会话（多用户）维度**的分组统计
（谁跑得多、谁失败率高），便于团队排查。

> 数据全在本机内存中构建，不额外调用模型、不产生 Token 消耗，也不上传任何数据。

---

### 8.19 📏 版本限额速查

| 项目 | 社区版 | 专业版 | 企业版 |
|------|--------|--------|--------|
| 每日任务次数 | 100 次/天 | 不限 | 不限 |
| 工作区数量 | 3 个 | 30 个 | 不限 |
| 知识库 | 5 文档 / 10MB | 不限 / 200MB | 全部不限 |
| 观测中心 | 当前任务实时 DAG（只读） | + 实时看板 / 历史 200 次 / 导出 | + 会话维度分组 / 历史 2000 次 |

顶栏 `📅 n/100` 徽标显示当日任务用量（跨天自动清零，重启服务不清零）；
超限时任务返回友好提示，升级后自动解除。`GET /api/quota` 可编程查询。

---

## 9. 命令行与 REPL

不开浏览器也能用。安装后命令行直接可用 `automind` 命令：

```bash
# 单次任务（默认工作模式）
automind "创建一个 requirements.txt 包含 fastapi 和 uvicorn"

# 指定模式 / 模型 / 项目目录
automind --mode react --provider deepseek --model deepseek-chat -p D:\myproj "修复 main.py 的报错"

# 信息查询
automind --version          # 版本
automind --list-tools       # 可用工具
automind --list-skills      # 可用技能
automind --list-providers   # 支持的提供商
```

### Rich REPL（交互式终端）

不带参数运行 `automind` 进入彩色交互终端：

```
+---------------- REPL ----------------+
| AutoMind v1.2.0                      |
| 模式: plan_and_execute · 模型: ...    |
+--------------------------------------+
automind> /help
```

REPL 内命令：

| 命令 | 功能 |
|------|------|
| `/help` | 帮助 |
| `/mode react` 或 `/mode plan_and_execute` | 切换执行模式 |
| `/tools` / `/skills` | 表格列出工具/技能 |
| `/stats` | 本次会话任务数与 Token 统计 |
| `/clear` | 清屏 |
| `/exit` | 退出（显示 Token 总量摘要） |
| 其他任何输入 | 作为任务执行，结果以 Markdown 面板渲染 |

---

## 10. 部署与运维（团队/生产）

### 10.1 Docker 部署

```bash
docker compose up --build
# 数据（配置/记忆/检查点）持久化在 automind_data 卷
```

镜像特性：非 root 用户运行、`/data` 工作卷、自带健康检查（打 `/api/health`）。

### 10.2 生产加固环境变量（默认全关，按需开启）

| 变量 | 作用 | 建议 |
|------|------|------|
| `AUTOMIND_AUTH_TOKEN` | 所有 API 与 WebSocket 需带 Bearer 令牌 | 暴露公网必开 |
| `AUTOMIND_CORS_ORIGINS` | 收紧跨域来源（逗号分隔） | 配合前端域名 |
| `AUTOMIND_MAX_CONCURRENT` | 并发任务上限（默认 8，超出返回 429） | 按机器性能调 |
| `AUTOMIND_RATE_LIMIT` | `/api/run` 每客户端每分钟次数（0=关） | 公网建议 30 |
| `AUTOMIND_REDACT_SECRETS` | 任务输出中的 API Key/密码自动打码 | 多人环境建议开 |
| `AUTOMIND_ALLOWED_ORIGINS` | WebSocket Origin 白名单 | 防跨站劫持 |

`docker-compose.yml` 中已备好全部变量的注释模板，取消注释即用。

### 10.3 运维接口与日志

- `GET /api/health`（无需鉴权）：版本 / 运行任务数 / 并发上限 / 运行时长，供探活。
- 结构化日志：安装 `structlog` 后输出 JSON 结构化日志；未安装则自动降级为
  标准库格式（`event key=value`），无需任何配置。
- 服务退出时自动释放 MCP 连接 / 向量库 / 模型连接池。

### 10.4 企业级安全与合规（企业版）

以下能力由企业版许可证解锁，社区版访问对应端点返回 403 + 升级提示：

**🔐 SSO / LDAP 单点登录**（`POST /api/auth/login`）

用户名/口令经企业目录校验后签发**短期会话令牌**（默认 8 小时），
HTTP 与 WebSocket 均可用该令牌访问，与 `AUTOMIND_AUTH_TOKEN` 静态令牌并存。
服务端配置（`.automind_config.json`）：

```json
{"sso": {
  "mode": "ldap",
  "session_hours": 8,
  "ldap": {"server": "ldap://dc1:389",
            "user_dn": "uid={username},ou=people,dc=corp,dc=com",
            "role_map": {"alice": "admin"}, "default_role": "viewer"}
}}
```

小团队可用 `"mode": "local"` + 内置用户目录（SHA-256 口令哈希），免 LDAP。
LDAP 模式需 `pip install ldap3`。

**🧩 细粒度权限 RBAC**（`/api/rbac`）

角色 × 动作矩阵，动作支持通配（`tool:*`、`api:*`）。内置三角色：

| 角色 | 权限 |
|------|------|
| `admin` | 全部（`*`） |
| `developer` | 执行任务 + 全部工具 + 历史/回滚（不可改配置与管理端点） |
| `viewer` | 只读（历史查看） |

可通过 `POST /api/rbac/role` 自定义角色；SSO 登录返回的角色与之联动，
`GET /api/rbac/check?role=&action=` 供网关/代理做统一判定。

**🚪 私有模型网关**（`/api/gateway`）

把全部 LLM 流量收敛到企业自建的 OpenAI 兼容网关（vLLM / LiteLLM / OneAPI）：
启用后所有提供商的 `api_base` 统一指向网关（原地址自动备份，**停用一键恢复**）；
`allowed_models` 白名单限定可用模型；网关 Key 建议用环境变量注入
（`api_key_env`），避免落盘。

---

### 10.5 🗄 数据与存储（v1.1：SQLite）

v1.1 起高频数据从零散 JSON 平面文件收敛到 **SQLite**（WAL 模式，并发安全、
增量写入）。首次启动自动把旧 JSON 数据一次性导入，**旧文件原地保留作备份**。

| 数据 | 存储位置 | 说明 |
|------|----------|------|
| 任务历史 / 团队任务 / 每日限额 | `.automind/automind.db` | 历史滚动上限 200 条 |
| 对话会话（多用户隔离） | `.automind/sessions.db` | 每会话最近 200 条消息 |
| 知识库（文档/片段/向量/检索日志） | `.automind/kb/kb.db` | 增量落库，检索走内存热路径 |
| 配置 / API Key / 工作区 | `.automind_config.json` | 保持纯文本 JSON，可手工编辑 |

**备份与迁移**：停止服务后整体拷贝 `.automind/` 目录与 `.automind_config.json`
即完成全量备份，放回新机器原位即可恢复。SQLite 文件可用 `sqlite3` CLI 或
DB Browser 等工具直接查看。

---

## 11. 常见问题 FAQ

**Q：顶部模型徽标一直是红色「未配置」？**
A：还没配 API Key。左下角「⚙ 设置」→「🔑 API Keys」为当前提供商填 Key 并「测试连接」。

**Q：徽标黄色带 ⚠？**
A：有 Key 但初始化失败。常见原因：Key 失效、`api_base` 写错、网络不通。
「测试连接」会告诉你卡在哪个阶段。

**Q：任务卡住不动 / 想中断？**
A：点输入区 ■ 或按 `Ctrl+.`。循环模式还会在收敛/空转时自动停止。
中断后对话区会出现「▶ 继续此任务」，从断点续跑不重做（见 8.10）。

**Q：Agent 把我的文件改坏了怎么办？**
A：右栏「↩️ 文件改动」列出它改过的每个文件，单个撤销或「全部回滚」
一键恢复原始内容（新建的文件会被删除）。长期项目建议同时用 git 兜底。

**Q：右栏的 💰 成本准吗？**
A：是**估算**：按内置单价表（公开定价）× 实际 token 数计算。用中转代理
或价格有变时，点击成本数字自定义你的实际单价即可。

**Q：为什么它说「文件不存在」？**
A：检查右上角 📁 项目目录是否指向了正确位置——所有相对路径都以它为根，
且出于安全不允许访问目录之外的文件。

**Q：担心它乱删文件？**
A：三层保险：① 审批切到「🙋 询问」逐一批准；② 高危命令（rm -rf 等）被硬拦截；
③ 事后在「🛡️ 安全审计」核查每一次调用。

**Q：换台电脑打开，聊天记录不见了？**
A：正常。会话按浏览器隔离（多用户设计）。记录都在服务器本地 `.automind/chats/` 下。

**Q：Token 花得快？**
A：① 在设置菜单「🖥 模型配置」给对话模式配便宜模型；② 「📊 统计分析」看 Token 效率定位大户任务；
③ 任务描述精准、避免让它反复试错。

**Q：协同/循环按钮带 🔒 点不动、定时任务打开是升级卡片？**
A：这些是**专业版**功能，当前运行的是社区版。安装商业扩展包 `automind-pro`
并配置许可证（环境变量 `AUTOMIND_LICENSE`，或项目根目录 `.automind_license` 文件）
后重启服务即自动解锁；左上角徽标会从「社区版」变为「专业版/企业版」。
许可证无效或过期会自动降级社区版，不影响其他功能。

**Q：社区版功能会被砍吗？**
A：不会。对话/工作/编程、全部工具/技能/MCP、安全体系（鉴权/限流/脱敏/审计）
承诺保持开源免费；详见 `docs/EDITIONS.md`。

**Q：想让它用我公司内部的工具？**
A：三选一：写 Python 技能（最简单，见 examples/03）；架 MCP 服务器（标准协议，生态互通）；
写插件（适合旁路逻辑如通知/审计）。

---

## 12. 附录：速查表

### 五种模式

| 模式 | 引擎 | 一句话 |
|------|------|--------|
| 💬 对话 | 直连 LLM | 只聊不动手 |
| ⚙️ 工作 | Plan-and-Execute | 先列计划再逐步干 |
| 💻 编程 | ReAct 循环 | 边看边改边验证 |
| 🤝 协同 | 多智能体编排 | 专家组分工协作 |
| 🔁 循环 | Loop Engineering | 迭代到达标为止 |

### 常用 API 端点（供二次开发）

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/health` | GET | 健康检查（免鉴权） |
| `/api/run` | POST | 执行任务 `{task, interaction, session_id}` |
| `/ws` | WebSocket | 流式任务执行 + 实时事件 + 审批交互 |
| `/api/status` | GET | 当前模式/模型/就绪状态 |
| `/api/chat/history` | GET/DELETE | 按会话读取/清空聊天记录 |
| `/api/stats` `/api/stats/detail` | GET | 基础/高级统计 |
| `/api/skills` `/api/skills/load` | GET/POST | 技能列表/目录导入 |
| `/api/mcp` | GET/POST/DELETE | MCP 服务器管理 |
| `/api/plugins/{name}/load` | POST | 加载插件 |
| `/api/schedule`（专业版） | GET/POST | 定时任务管理 |
| `/api/audit` | GET | 安全审计日志 |
| `/api/audit/export`（专业版） | GET | 审计报告导出（PDF / 打印版） |
| `/api/templates/custom`（专业版） | GET/POST/DELETE | 自定义模板管理 |
| `/api/workspaces` `/api/workspaces/switch` | GET/POST | 工作区管理与切换 |
| `/api/history` `/api/history/{id}` | GET/DELETE | 任务历史（持久化，可回溯） |
| `/api/changes` `/api/changes/rollback` | GET/POST | 文件改动列表 / 撤销回滚 |
| `/api/auth/login`（企业版） | POST | SSO/LDAP 登录，签发会话令牌 |
| `/api/rbac` `/api/rbac/check`（企业版） | GET/POST | 角色权限矩阵 / 判定 |
| `/api/gateway`（企业版） | GET/POST | 私有模型网关配置 |
| `/v1/chat/completions` | POST | OpenAI 兼容对话补全（Agent 集成，支持 SSE 流式） |
| `/v1/models` | GET | OpenAI 兼容模型列表 |
| `/api/integrations/continue` | GET | 生成 Continue.dev 即贴即用配置 |
| `/api/files/tree` `/api/files/read` `/api/files/write` | GET/POST | 代码编辑器：文件树/读/写（限项目根） |
| `/api/changes/diff` | GET | 文件改动 Diff 数据（前像 vs 当前） |
| `/api/experts` 系列 | GET/POST/DELETE | 专家市场：目录/安装/创建/激活（进阶功能专业版） |
| `/api/team/tasks` 系列 | GET/POST/DELETE | 团队任务分配看板 |

### 文件位置

| 内容 | 位置 |
|------|------|
| 配置与 API Keys | 项目目录 `.automind_config.json` |
| 聊天记录 | `.automind/chats/<会话ID>.json` |
| 任务历史（可回溯） | `.automind/task_history.json` |
| 自定义模板（专业版） | `.automind/custom_templates.json` |
| 长期记忆（向量库） | `.automind/chroma/` |
| 检查点 | `.automind/checkpoints/` |
| 插件 | `~/.automind/plugins/` |
| 教程示例 | 仓库 `examples/` 目录 |

---

## 13. 更新日志

每次升级带来了什么，按版本倒序。完整记录见仓库 [CHANGELOG.md](CHANGELOG.md)。

### v1.5.2 — 2026-08-12 ｜ 当前版本

开箱即用 — 第一次跑就会撞上的 7 个问题

- **🖥 命令行启动不再直接崩溃**：中文版 Windows 的命令行窗口默认用 GBK 编码，而界面里的
  方框线、✅ 这类符号它显示不了 —— 此前不是显示成乱码，而是**程序当场退出并甩出一串
  英文报错**。按说明书敲 `python -m automind.server` 或 `automind` 的用户，第一步就会
  卡在这里。现在程序会自己把命令行切到能显示中文的模式（**退出时自动还原**，不影响你在
  同一个窗口里跑的其它程序）。
- **🔑 只配了一家的 Key 也能直接用**：软件出厂默认用 OpenAI 的模型，但大多数人只填了
  DeepSeek 的 Key，于是一打开就提示"模型连接失败"，**却不告诉你要去哪儿改**。现在会
  自动认出你填了哪家的 Key 并直接用它，同时明确告诉你："未检测到 OpenAI 的 API Key，
  已检测到 DeepSeek Key，将使用 deepseek-chat"。你要是自己在设置里指定过某一家，
  则不会被擅自改掉，只会提醒你 Key 还没填。
- **🔀 多个标签页同时跑任务不再互相干扰**：此前所有并发任务共用同一份运行状态 ——
  一个标签页在"对话"、另一个在"循环编程"，模式会**互相覆盖**，两边的上下文互相串进
  对方的对话，Token 统计也会被对方清零，而且没有任何提示。现在每个会话都有自己独立的
  运行环境（共用的只是工具、技能这些只读部分，所以几乎不占额外开销）。
- **⚡ 切换模式/模型不再卡 2~3 秒**：此前在界面上点一下模式切换，程序会把整个引擎重建
  一遍（重新扫描项目、重建记忆库、重新加载全部工具与技能），而实际变化的只有一个模型名。
  现在只替换模型本身，**点击即时生效**（实测由 1.1 秒降到 1 毫秒以内）。
- **🚀 文件浏览与编辑不再拖慢全局**：打开目录选择器、预览文件、保存编辑器内容时的读写
  磁盘操作，此前会把服务端的主循环整个占住 —— **连正在逐字输出的对话也会一起卡住**。
  现在这些操作都挪到后台线程。
- **📦 `automind` 命令可用**：此前照说明书敲 `automind` 会提示"不是内部或外部命令"。
  现在已随包安装，命令行用法与文档一致。
- **🔌 端口被占用会说清楚，不再让你看着旧版本发懵**：如果已经有一个 AutoMind
  （多半是安装好的桌面版）在跑，新启动的那个其实**根本没起来** —— 但屏幕上最后一句
  仍是"打开浏览器访问 http://127.0.0.1:8765"。你照做，看到的是那个一直在跑的旧版本，
  于是以为"更新没生效"。现在启动前会先检查端口，并直接告诉你："端口 8765 已被占用，
  占用者 AutoMind v1.5.0（桌面版安装包）；现在用浏览器打开看到的是它，而不是你刚启动的
  v1.5.2"，并给出换端口的命令。

### v1.5.1 — 2026-08-09

可靠性与可观测性 — 不再"悄悄出错"

- **💬 聊天记录不再悄悄丢失**：此前保存失败是**完全静默**的，你下次打开发现对话没了
  也无从查起。现在会自动重试，仍失败则在界面上明确告知（并提示检查磁盘空间与目录
  权限）；**不会因此中断正在进行的任务**。
- **⏱ 任务卡住会自己停下**：模型服务"连上了却不再回话"时，此前任务会**无限期挂着**，
  界面停在"执行中"永远不动。现在单轮超过 5 分钟自动中止并说明原因；执行期间每几秒
  刷新一次状态，显示**"正在思考 / 正在生成回答 已 N 秒"**，让你分得清"在想"和"挂了"。
- **🪙 Token 数实时更新**：此前要等整段回答生成完才跳一次，长回答期间一直显示 0，
  看起来像没在计费。现在每次模型调用结束就更新。
- **🛡 用量超预算会真正拦住**：预算保护此前**一直是空转的**（代码建好了却从未被调用）。
  现在用到 80% 会提醒并自动压缩上下文，用满则停止调用，不会无声无息地一直花钱。
- **⛔ 坏掉的工具不再反复重试**：同一个工具连续失败 3 次即停用，失败原因在对话区
  **标红显示**并附上连续失败次数 —— 此前它会一直重试到步数耗尽，既慢又看不出发生了
  什么。任务开始前还会自检模型配置与目录权限。
- **🧭 计划出现循环依赖能自动化解**：此前检测到循环却不做处理，任务会直接报错中止。

### v1.5.0 — 2026-08-07

新增 12 个办公与集成工具 — AI 现在能直接帮你处理表格、文档、邮件

- **📊 办公四件套**：**Excel**（读写表格、增删行、和 CSV 互转）、**Word**（读取内容、
  生成文档、插入表格）、**PDF**（提取文字、合并、拆分、旋转）、**邮件**（发送邮件、
  收取和查看收件箱）。直接说"把这个表格里的数据汇总成一份 Word 报告"就行。
- **🔧 通用五件套**：**联网搜索**、**网页/接口请求**、**数据库查询**、
  **文件检索**（按文件名或内容找）、**压缩解压**（zip / tar.gz）。
- **🤝 协同三件套**：**桌面通知**（长任务跑完弹窗提醒）、**日历**（ICS 日程文件，
  Windows 上还能直接写进 Outlook）、**群机器人**（钉钉 / 企业微信 / 飞书 / Slack）。
- **📦 按需安装，不臃肿**：办公功能依赖的库**不会**随主程序一起装。桌面版已内置；
  用 pip 安装的用户，第一次用到时会提示一行安装命令，照抄即可
  （`pip install "automind-agent[office]"`）。
- **🔐 外发动作一律需要你点确认**：发邮件、发群消息属于「发出去就收不回」的操作，
  默认每次都要经过审批。另外**刻意不提供群发功能**（收件人上限 20 人）——
  能被 AI 驱动的无限群发就是垃圾邮件工具，专业版同样不会解锁。
  邮箱密码只从环境变量读取，不会出现在任何记录里。
- **🛡 联网与解压的安全防护**：网络请求默认**禁止访问内网地址**（防止 AI 被网页内容
  诱导去读取你的内网服务或云端凭据）；解压会校验压缩包内的路径，防止文件被释放到
  目录之外。
- **⭐ 社区版与专业版的分界**：分界线划在**具体动作**上，不是整个工具 —— 社区版能真正
  把表格读出来、把报告写出去。进阶动作（Excel 样式美化、Word 模板套打、PDF 加水印与
  加密、企业数据库读写）属专业版。

### v1.4.6 — 2026-08-07

堵上一条上版没能覆盖到的跨站通道 — 建议立即升级

- **🚨 阻止外部网页通过 WebSocket 操纵本机**：上一版收紧了跨站访问，但 **WebSocket
  走的是另一条路，不受那套规则约束** —— 也就是说，你访问的任何网页仍可能在后台连上
  本机 AutoMind，让它执行任务、读写文件。现已改为**默认只接受本机页面的连接**，
  外部来源一律拒绝并记录日志。命令行与 SDK 调用不受影响；只在本机使用无需额外设置。

### v1.4.5 — 2026-08-06

安全加固第二轮 — 承接 v1.4.4，建议一并升级

- **✅ 「询问」模式的最后两个漏口**：上一版只补了一处，另外两条执行路径（编程模式的
  ReAct 循环、工作模式的计划执行）仍会在**没有审批通道**或**审批出错**时直接放行 ——
  需要你点「批准」的操作被悄悄执行了。现已全部改为**问不到人就不做**，并在界面上说明原因。
  （若希望无人值守运行，请把审批模式设为「自动」或「全批准」—— 这是明确的授权表达，
  而不是靠配置疏漏放权。）
- **🌐 收紧跨站访问（重要）**：此前的配置会让**你浏览的任何网页**都能在后台调用本机
  AutoMind 并读取结果 —— 包括让它执行任务。而本地默认又不开鉴权。现在默认只信任
  本机页面；需要其它来源请显式配置 `AUTOMIND_CORS_ORIGINS`。
- **📁 目录浏览限定范围**：选择项目目录时不再能浏览整块磁盘，只限**主目录、当前项目与
  已配置的工作区**（可用 `AUTOMIND_FS_ROOTS` 追加）。实测 `C:\` 下可见目录从 22 个收敛到 1 个。
- **🔑 开放到局域网时自动开启鉴权**：以 `--host 0.0.0.0` 启动而又没设访问令牌时，系统会
  **自动生成一个随机令牌**并在启动信息里醒目打印（同时保存下来）。只在本机使用则完全
  不受影响，无需任何额外操作。
- **🔒 IDE 接入配置接口**：该接口会返回**明文访问令牌**，现在未设令牌时只允许本机读取，
  并禁止被缓存。

### v1.4.4 — 2026-08-06

安全加固专版 — 强烈建议升级

- **🔒 代码沙箱重写（最高危）**：此前 Agent 执行的「Python 代码」**能跑出沙箱**，
  读写你机器上的任意文件、甚至执行任意系统命令。已重写为**三层防御**：执行前静态检查、
  受限运行环境、并放到**独立子进程**里跑。另外修复了「超时不生效」—— 死循环代码此前会把
  程序卡住无法恢复，现在会被真正终止。
  （说明：沙箱防的是"AI 写出的代码越权"，不等于可以放心执行来路不明的代码；
  确有此需要请在容器/虚拟机中部署。）
- **💉 修复测试技能的命令注入**：运行测试时的文件匹配参数由 AI 填写，此前会被当成系统
  命令的一部分执行，构造特殊内容可执行任意命令。现在改为**参数化调用**（不经过命令行
  解释器）并做严格字符校验。
- **✅ 修复「询问」模式可能被静默跳过**：审批通道出错时（最常见的是**你关掉了页面**），
  系统此前会**默认放行**，等于「询问」模式悄悄变成了「全批准」。现在一律按**拒绝**处理，
  并在界面上说明原因。命令行审批里「直接回车」也不再等于同意。
- **📁 收紧目录浏览接口**：此前任何人都能通过接口列举本机任意目录。若你把服务开放到
  局域网又没设访问令牌，等于把磁盘目录结构公开了。现在**未设令牌时只允许本机访问**，
  并禁止浏览系统敏感目录。本机选择项目目录不受影响。

### v1.4.3 — 2026-08-05

修复输入框被挤出界面 · 任务历史可搜索 · 执行进度看得见

- **🩹 修复长会话把输入框挤出界面**：消息一多，对话面板会被内容撑破容器，消息区不再
  滚动、输入框被顶到视口之外彻底看不见。实测 80 条消息时面板高度被撑到 **11821 像素**
  （可用高度仅 648）。已修复：同样 80 条消息下消息区正常滚动、输入框稳定停在底部。
- **🔍 任务历史可搜索、可翻页**：新增**关键词搜索**（任务内容与产出一起匹配，命中处高亮）、
  **模式筛选**与**成功/未完成筛选**；列表改为**分页**（每页 20 条），不再一次性把全部记录
  铺出来。另外，此前界面只能看到最近 **50** 条，而系统实际保留 **200** 条 ——
  剩下的记录够不着，现在都能搜到了。
- **⏱ 执行进度看得见**：输入框上方新增一条轻量进度指示 —— **当前阶段 · 第 X / N 步 ·
  正在做什么 · 已耗时**，右侧「查看详情」可直接跳到观测中心看完整执行过程。解决
  "长任务跑着跑着，界面看起来像卡住了"。对话与编程模式没有预先生成的计划，此时
  **不显示编造的百分比**，改用流动条表示"正在进行、总量未知"。

### v1.4.2 — 2026-08-05

消息可编辑/删除 · 断线可视化 · 恢复语义纠正 · 界面精修

- **✎ 消息编辑与删除**：把鼠标移到任意气泡上，右上角浮出操作条 —— **复制 / 编辑 / 删除**。
  此前发错问题只能"复制原文 → 手动改 → 重发"。编辑用内联输入框（`Enter` 发送、
  `Esc` 取消），重发时会自动移除该条及其之后的消息，因为问题都改了、基于旧问题的
  回答留着只会让上下文自相矛盾。删除有二次确认，且只影响本地会话记录，
  **不会回滚已产生的文件改动**。
- **🔌 断线时看得见"正在重连"**：新增顶部横幅，显示**重连倒计时**与已尝试次数，
  可点「立即重连」跳过等待；顶栏徽标区分「⟳ 重连中」与「○ 未连接」；重连成功会给出提示。
- **⚠ 任务执行中断线不再卡死**：连接一断结果就送不回来了，界面此前会一直停在"执行中"、
  输入框永久禁用。现在会落一张失败卡片（带重跑入口）并解锁界面。
- **🩹 「继续此任务」改名，避免误导**：经核对，本软件**没有真正的断点续传** ——
  检查点只在任务成功结束后才写，任务失败时并不会留下检查点。按钮改为
  **「▶ 检查现状后接着做」** 与 **「↻ 从头重跑」**，并注明：两者都会重新执行一次任务，
  已写出的文件不会被回滚。
- **🔢 修复升级日志里的退出码记错**：可见模式重试安装成功时，安装日志却仍记录为失败码。
- **🎨 界面精修**：气泡双层阴影 + 悬停轻微抬起、头像内描边、滚动条静止时隐入、
  输入区聚焦提亮、数字等宽字形；补上全局键盘焦点环。

### v1.4.1 — 2026-08-05

修复"点升级后应用消失" · 输入框草稿保护 · 任务失败可一键恢复

- **🛠 修复升级后应用不再启动**：桌面版是无控制台的 GUI 进程，升级脚本又以分离方式
  启动，此前没有显式指定标准输入输出句柄 —— 脚本里的重定向和管道全部失效，批处理
  在装之前就退出了，于是"应用退出、再也没回来"。现已修复，并给脚本加了三重兜底：
  静默安装失败自动改用可见模式重试（装在 `Program Files` 需要 UAC 提权）、安装前
  清理残留实例、**无论成败最后都把应用拉起来**。安装日志与执行轨迹留在临时目录便于追查。
- **✍ 输入框草稿自动保存**：打到一半切模式、切视图、关窗口，内容不再丢失，重新打开
  自动回填（按会话与工作区隔离）。语音听写与模板填入的内容一并保护。
- **🎯 打开即聚焦**：进入对话区、切换模式、任务跑完后自动把光标交还输入框，省掉
  "先点一下输入框"这步；有弹窗时不抢焦点。
- **⧉ 自己发的提问也能复制**：此前复制按钮只挂在 AI 回复上。同时改为复制 **Markdown
  原文**（旧实现取气泡可见文本，会把复制按钮自己的图标一起带走），并在非 HTTPS 环境下
  自动回退，局域网访问时复制不再失灵。
- **🚑 任务失败有了恢复入口**：失败/中断从"一行红字"改为一张卡片 —— 可展开完整报错、
  **▶ 继续此任务**（从中断处续跑，不重做已完成部分）、**↻ 重新执行**、**⧉ 复制错误**，
  并针对 Key 失效、限流、超时、网络不通、上下文超长等常见原因给出下一步建议。
  任务原文随卡片一起留存，**重启应用后依然能续跑**。
- **📖 手册新增本章**：更新日志现可在手册内直接查阅。

### v1.4.0 — 2026-07-30

四项体验增强：字号调节 · 无闪烁切换 · 完整快捷键系统 · 任务完成通知

- **🔤 字号调节**：85%~130% 无级调节，`Ctrl+=` / `Ctrl+-` / `Ctrl+0` 可直接调，
  "标准"档与升级前像素级一致。
- **✨ 消除切换时的黑屏闪烁**：对话面板改为常驻挂载、只切显示，DOM 不再重建，顺带修复
  切回对话时滚动位置丢失；新增「减少动效」开关。
- **⌨ 完整快捷键系统**：新增 19 项全局快捷键，按 `?` 查看；帮助弹窗内容由注册表直接
  生成，与实际生效的按键同源。
- **🔔 任务完成桌面通知**：仅在窗口不可见时提醒，点通知跳回应用，完成/失败/中断三种
  终态都会通知。

### v1.3.2 — 2026-07-29

三平台安装包 · 兼容版界面补齐升级提示

- 首个三平台同时发布的版本：Windows（代码签名）/ macOS（通用二进制 DMG）/ Linux。
- 修复 PyPI 元数据死链与许可字段。
- 兼容版界面补齐升级提示与检查更新入口。

> 💡 应用内点击左下角 **「⚙ 设置 → 检查更新」** 可查看是否有新版本并一键升级；
> 升级过程中应用会短暂退出，安装完成后**自动重新启动**。


---

> 手册版本与软件 v1.5.2 同步。遇到手册未覆盖的问题，可查看
> [README.md](README.md)（中文）/ [README.en.md](README.en.md)（English），
> 或直接在 💬 对话模式里问 AutoMind 本身。
